NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server only — never expose to the browser (Dashboard → Settings → API → service_role)
SUPABASE_SERVICE_ROLE_KEY=
# Comma-separated display names that can open /admin (server only)
ADMIN_USER_NAMES=
